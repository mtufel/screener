# Telegram Bot Configuration
TELEGRAM_BOT_TOKEN=your_bot_token_here
TELEGRAM_CHAT_ID=your_chat_id_here
TELEGRAM_REPLY_MODE=thread  # 'thread' (channel comments via discussion group) or 'reply' (direct feed reply)

# Whitelist Targets (BTC, ETH, SOL)
COINS_WHITELIST=BTC,ETH,SOL

# Screener Interval & Ranking Settings
SCAN_INTERVAL_MINUTES=5
TOP_N_ALERTS=10

# Market Data Provider Configuration
# Options: binance, binance_spot, hyperliquid, oanda, ccxt (or ccxt_<exchange>)
DATA_PROVIDER=binance
FALLBACK_DATA_PROVIDER=hyperliquid

# CCXT Configuration (used when DATA_PROVIDER=ccxt)
# Supported exchanges: binance, bybit, okx, hyperliquid, gateio, kraken, etc.
CCXT_EXCHANGE=binance

# Hyperliquid API Settings (Gentle rate-limiting)
HYPERLIQUID_API_URL=https://api.hyperliquid.xyz/info
MAX_CONCURRENT_REQUESTS=3
REQUEST_TIMEOUT_SECONDS=20
RATE_LIMIT_RPS=3.0

# Strategy Timeframes & Lookback
HTF_TIMEFRAME=4h
LTF_TIMEFRAME=5m
EXTREME_LTF_TIMEFRAME=5m
LOOKBACK_CANDLES=500
MAX_HTF_RETRACE_CANDLES=18
USE_CLOSE_BASED_INVALIDATION=false
SESSION_FILTER_ENABLED=false

# Strategy Enablement (true / false)
ENABLE_STRATEGY_2=true

# Strategy 2 (Extreme LTF FVG) — Invalidation & Sessions
EXTREME_USE_CLOSE_INVALIDATION=true     # 'true' = invalidate on candle close, 'false' = wick
EXTREME_SESSION_FILTER_ENABLED=false    # 'true' = restrict FVG formation to EXTREME_SESSIONS
EXTREME_SESSIONS=ALL                    # ALL, NY, LONDON, LONDON,NY, or 'HH:MM-HH:MM' (UTC)

# Strategy 2 (Extreme LTF FVG) — Research-backed Bias Filters (2026-09 marginal analysis)
# Max distance (%) of the LTF FVG away from the active 4H anchor zone. 0 disables.
EXTREME_MAX_DIST_FROM_4H_PCT=2.0
# Require a strong directional impulse candle (c2 body >= 50% of range). 'true' = opt-in.
EXTREME_REQUIRE_MOMENTUM=false
# Reject LTF FVGs whose gap_pct exceeds this ceiling (%). 0 disables.
EXTREME_MAX_GAP_PCT=0.3
# Reject LTF FVGs forming more than this many candles after the 4H touch. 9999 disables.
EXTREME_MAX_LTF_FVG_AGE_CANDLES=9999

# Application Logging (DEBUG, INFO, WARNING, ERROR)
LOG_LEVEL=INFO
