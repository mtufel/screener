## Tasks: ccxt-integration-wrap

1. Write `specs/ccxt-integration.md` — done
2. Write `design.md` — done
3. Create `ccxt_ws.py` (websocket adapter)
4. Create `exchange_adapter.py` (ExchangeAdapter class)
5. Update config schema (`config_schema/` or `.env`) for ccxt settings
6. Integrate adapter into `backtest_extreme_fvg.py` and `live_screener_extreme.py`
7. Test live fetch + backtest loop
8. Commit with attribution; open PR to develop
